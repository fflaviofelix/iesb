/*Preencha as linhas faltantes do código para que o resultado da execução desse programa seja 3, 14, 18.*/

int funcao(int * x, int * y, int z);

int main()
{
    int a,b,*c,x;

    a = 2;
    b = 3;
    c = &a;

    x = funcao(&a, c, b);
    return 0;
}


int funcao(int * x, int * y, int z);

{
    int *a,*b,c, x;
    x = z + c
    z = *x + *y
    return z;

}
