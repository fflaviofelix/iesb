/*Construa uma função que recebe por parâmetro um número e retorne 1 caso ele seja perfeito ou zero caso ele não seja perfeito. Um número perfeito é um número que a soma de seus divisores com exceção do próprio número é igual a ele mesmo. Ex.: 6(1+2+3), 28(1+2+4+14). Na sua funçaõ principal , solicite 10 números ao usuário e após lidos os 10 números, diga quantos e quais são perfeitos.*/

#include <stdio.h>
#include <stdlib.h>

int main()

{
    int i, x, divisor;
    numero = 0
    for (i, i <=10, i++) 
        {printf("Informe número: ");
        scanf("%d", x);
        if ((x % (x - 1) == 0 )
            printf(x) 
        i = i + 1;}
}
